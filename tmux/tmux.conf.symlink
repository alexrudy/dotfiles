set-option -g default-shell $SHELL
set-option -g set-titles on
set-option -g set-titles-string "#W - #{session_name} - #h"

set-option -g status-bg black
set-option -g status-fg green
set -g default-terminal "xterm-256color"
