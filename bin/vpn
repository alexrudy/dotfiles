#!/usr/bin/env zsh

/usr/bin/osascript <<EOF
tell application "Tunnelblick"
    connect "ucolick-only"
    get state of first configuration where name = "ucolick-only"
    repeat until result = "CONNECTED"
        delay 1
        get state of first configuration where name = "ucolick-only"
    end repeat
end tell
EOF